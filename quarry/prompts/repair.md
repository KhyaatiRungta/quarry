That tool call failed ({failure_kind}).

{observation}

This is repair attempt {attempt}. Diagnose the actual cause from the output above,
then send one corrected tool call. If a column name or dtype is the problem, call
inspect_schema rather than guessing again.
